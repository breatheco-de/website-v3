name: chart - default
yaml: |
  - type: chart
    version: "1.0"
    source: |
      flowchart LR
        A[Prompt] --> B[Model] --> C[Answer]
    caption: How a prompt becomes an answer
    speed: 1
